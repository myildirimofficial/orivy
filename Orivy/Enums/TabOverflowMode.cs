public enum TabOverflowMode
{
    /// <summary>
    /// Tabs are compressed (scaled down) to fit the available width.
    /// </summary>
    Collapse,

    /// <summary>
    /// Tabs keep their natural width; overflow is accessible via chevron buttons.
    /// </summary>
    Scroll
}